<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <!-- Permissions required by the conductor triggers. The config plugin adds these
       to the host app's manifest; they are declared here for the library too. -->
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
  <uses-permission android:name="android.permission.WAKE_LOCK" />
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
  <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

  <application>
    <!-- Exact-alarm trigger receiver. -->
    <receiver
      android:name=".triggers.ConductorAlarmReceiver"
      android:exported="false" />

    <!-- Reschedules persisted alarms after a device reboot. -->
    <receiver
      android:name=".triggers.BootReceiver"
      android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
      </intent-filter>
    </receiver>
  </application>
</manifest>
